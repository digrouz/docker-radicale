{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "radicale.fullname" . }}-config
  labels:
    {{- include "radicale.labels" . | nindent 4 }}
data:
  config: |
    {{- .Values.config | nindent 4 }}
{{- end }}
